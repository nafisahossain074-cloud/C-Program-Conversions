#include <stdio.h>

int main() {
    char ch;
    int num;

    printf("Enter a character: ");
    scanf("%c", &ch);

    // Get ASCII value
    num = (int)ch;

    printf("The ASCII value of the character is: %d\n", num);

    return 0;
}
