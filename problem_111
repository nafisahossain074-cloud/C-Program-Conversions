#include <stdio.h>

int main() {
    int num;
    char ch;

    printf("Enter a single-digit integer (0-9): ");
    scanf("%d", &num);
    ch = num + '0';
    printf("The integer as a character is: %c\n", ch);

    return 0;
}
