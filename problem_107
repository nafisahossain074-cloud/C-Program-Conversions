#include <stdio.h>

int main() {
    float num;
    char str[50];
    printf("Enter a float number: ");
    scanf("%f", &num);
    sprintf(str, "%f", num);

    printf("The float as a string is: %s\n", str);

    return 0;
}
